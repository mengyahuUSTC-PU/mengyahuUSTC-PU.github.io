# automation/

Automation pipeline for the blog (built out in phases 2–5).

- `scripts/` — fetch, publish, and analytics scripts (Python), run by cron on the Azure VM
- `prompts/` — prompt files for topic selection, briefing writing, and deep-dive writing
- `reports/` — weekly reports (Markdown, named by date)
