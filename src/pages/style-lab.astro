---
// Hidden typography lab — not linked from anywhere, noindex.
// Shows the same hero + card + article excerpt in three type treatments.
---

<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex, nofollow" />
    <title>Type Lab（内部对比页）</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Noto+Serif+SC:wght@500;600;900&family=Source+Serif+4:ital,opsz,wght@0,8..60,400..900&display=swap"
    />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/lxgw-wenkai-webfont@1.7.0/style.css" />
    <style>
      :root {
        --cream: #faf6ef; --ink: #1c1a17; --mut: #6f675a; --org: #c2500f;
        --card: #fffdf8; --line: #e7dfcf;
        --band-from: #33351f; --band-to: #241c10;
        --band-text: #f3ead8; --band-mut: #b9b09a; --band-org: #e8965a;
      }
      * { box-sizing: border-box; }
      body {
        margin: 0; background: #efe9dd; color: var(--ink);
        font-family: -apple-system, 'PingFang SC', sans-serif;
        line-height: 1.8; padding: 2.5rem 1rem 5rem;
      }
      .wrap { max-width: 52rem; margin: 0 auto; }
      .note { max-width: 40rem; margin-bottom: 2.5rem; }
      .note h1 { font-size: 1.5rem; line-height: 1.35; }
      .note p { color: var(--mut); font-size: 0.95rem; }
      .variant { margin-bottom: 3.5rem; }
      .variant > .label { font-weight: 700; font-size: 1.1rem; margin-bottom: 0.2rem; }
      .variant > .label b { color: var(--org); }
      .variant > .desc { color: var(--mut); font-size: 0.9rem; margin: 0 0 1rem; max-width: 42rem; }
      .demo { border-radius: 16px; overflow: hidden; border: 1px solid var(--line); box-shadow: 0 8px 26px rgba(40,32,20,0.10); }
      .hero {
        background: linear-gradient(140deg, var(--band-from), var(--band-to));
        color: var(--band-text); padding: 2.2rem 2rem 2.6rem;
      }
      .hero .name { font-size: 0.95rem; font-weight: 700; margin-bottom: 1.6rem; }
      .hero h2 { margin: 0; font-size: 2.3rem; line-height: 1.18; max-width: 26rem; }
      .hero h2 em { font-style: normal; color: var(--band-org); }
      .hero p { color: var(--band-mut); font-size: 0.95rem; max-width: 28rem; margin: 1rem 0 0; }
      .body-part { background: var(--cream); padding: 1.6rem 2rem 2.2rem; }
      .card { background: var(--card); border: 1px solid var(--line); border-radius: 14px; padding: 1.1rem 1.4rem; margin-bottom: 1.8rem; }
      .card .d { color: var(--org); font-size: 0.72rem; font-weight: 600; letter-spacing: 0.08em; }
      .card h3 { margin: 0.3rem 0 0.3rem; font-size: 1.15rem; line-height: 1.5; }
      .card p { margin: 0; color: var(--mut); font-size: 0.88rem; }
      .excerpt h4 { font-size: 1.35rem; margin: 0 0 0.6rem; line-height: 1.45; }
      .excerpt p { margin: 0; font-size: 1rem; max-width: 38rem; }

      /* V0 现状：系统黑体 */
      .v0 .hero h2, .v0 .card h3, .v0 .excerpt h4 {
        font-family: -apple-system, 'SF Pro Display', 'PingFang SC', sans-serif;
        font-weight: 800; letter-spacing: -0.03em;
      }

      /* V1 书卷：宋体标题 + 黑体正文 */
      .v1 .hero h2, .v1 .card h3, .v1 .excerpt h4 {
        font-family: 'Source Serif 4', 'Noto Serif SC', serif;
        font-weight: 600; letter-spacing: 0.01em;
      }
      .v1 .hero h2 { font-weight: 900; }

      /* V2 文楷：楷体标题 + 黑体正文 */
      .v2 .hero h2, .v2 .card h3, .v2 .excerpt h4 {
        font-family: 'LXGW WenKai', 'Source Serif 4', serif;
        font-weight: 700; letter-spacing: 0.01em;
      }

      .footer-note { max-width: 40rem; color: var(--mut); font-size: 0.92rem; border-top: 1px solid var(--line); padding-top: 1.6rem; }
      .footer-note b { color: var(--org); }
    </style>
  </head>
  <body>
    <div class="wrap">
      <div class="note">
        <h1>字体对比页（内部，不对外链接）</h1>
        <p>橄榄绿配色不变，只换「标题字体」。正文三个方案都保持系统黑体（苹方/微软雅黑）——正文用衬线在屏幕上会累，标题才是气质担当。</p>
      </div>

      <section class="variant v0">
        <div class="label">方案 <b>0</b> · 现状（系统黑体加粗）</div>
        <p class="desc">现在线上的样子，作为对照。</p>
        <div class="demo">
          <div class="hero">
            <div class="name">Mengya (Mia) Hu</div>
            <h2>从工程视角，<br />看 <em>Responsible AI</em></h2>
            <p>安全、评测与治理的第一手笔记。每篇中英双语，观点署名个人。</p>
          </div>
          <div class="body-part">
            <div class="card">
              <span class="d">2026年7月17日 · META</span>
              <h3>你好，世界：这个博客要写什么</h3>
              <p>开篇：为什么做这个双语博客，以及会写哪些内容。</p>
            </div>
            <div class="excerpt">
              <h4>「越狱」为什么屡禁不止</h4>
              <p>补丁式防御追不上组合式攻击——这不是工程投入不够，而是攻击面的数学结构决定的。要理解这一点，得先把「对齐」拆成训练时约束和推理时约束两层来看。</p>
            </div>
          </div>
        </div>
      </section>

      <section class="variant v1">
        <div class="label">方案 <b>1</b> · 书卷（宋体/衬线标题）</div>
        <p class="desc">中文标题用思源宋体、英文用衬线体。印刷品的书卷气，柔和但保持专业分量，和「文章页像印刷品」一脉相承。</p>
        <div class="demo">
          <div class="hero">
            <div class="name">Mengya (Mia) Hu</div>
            <h2>从工程视角，<br />看 <em>Responsible AI</em></h2>
            <p>安全、评测与治理的第一手笔记。每篇中英双语，观点署名个人。</p>
          </div>
          <div class="body-part">
            <div class="card">
              <span class="d">2026年7月17日 · META</span>
              <h3>你好，世界：这个博客要写什么</h3>
              <p>开篇：为什么做这个双语博客，以及会写哪些内容。</p>
            </div>
            <div class="excerpt">
              <h4>「越狱」为什么屡禁不止</h4>
              <p>补丁式防御追不上组合式攻击——这不是工程投入不够，而是攻击面的数学结构决定的。要理解这一点，得先把「对齐」拆成训练时约束和推理时约束两层来看。</p>
            </div>
          </div>
        </div>
      </section>

      <section class="variant v2">
        <div class="label">方案 <b>2</b> · 文楷（霞鹜文楷标题）</div>
        <p class="desc">开源楷体，笔画有手写的温度，是中文博客圈近年最受欢迎的字体。最柔和、最有个人气质，但正式感弱一些。</p>
        <div class="demo">
          <div class="hero">
            <div class="name">Mengya (Mia) Hu</div>
            <h2>从工程视角，<br />看 <em>Responsible AI</em></h2>
            <p>安全、评测与治理的第一手笔记。每篇中英双语，观点署名个人。</p>
          </div>
          <div class="body-part">
            <div class="card">
              <span class="d">2026年7月17日 · META</span>
              <h3>你好，世界：这个博客要写什么</h3>
              <p>开篇：为什么做这个双语博客，以及会写哪些内容。</p>
            </div>
            <div class="excerpt">
              <h4>「越狱」为什么屡禁不止</h4>
              <p>补丁式防御追不上组合式攻击——这不是工程投入不够，而是攻击面的数学结构决定的。要理解这一点，得先把「对齐」拆成训练时约束和推理时约束两层来看。</p>
            </div>
          </div>
        </div>
      </section>

      <div class="footer-note">
        <p>选定后我会把该字体接进全站（自托管字体子集，不拖慢加载），这个对比页会删除。回复 <b>0</b> / <b>1</b> / <b>2</b> 即可，也可以说「1 但再细一点」这类微调。</p>
      </div>
    </div>
  </body>
</html>
